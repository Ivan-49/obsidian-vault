`''.join(list)`  - принимает массив , и возвращает строку состоящую из элементов массива 

```python
list = ['new', 'old', 'age']
def start(list):
	str - ' '.join(list)
	return str

print(start(list))

>>> new old age
```

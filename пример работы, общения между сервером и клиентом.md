#programming
Сервер:
```python
import socket
socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
socket.bind(('127.0.0.1', 8888))
socket.listen()
user, addr = socket.accept()
client = (user.recv(1024)).decode()
while True:
    message = input()
    user.send(message.encode())
    message = user.recv(1024)
    message = message.decode()
    print(addr, ":", message)
```
Клиент:
```python
import socket
socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
socket.connect(('127.0.0.1', 8888))
socket.send('я тут'.encode())
while True:
    message = socket.recv(1024).decode()
    print(message)
    message = input()
    socket.send(message.encode())
```
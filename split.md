---
tags:
  - python
---

`.split('')` - метод для строк, принимает строку по которой разделит строку на элементы массива
```python
string = 'всем привет, как дела?'
string = string.split(' ')
print(string)

>>>['всем','привет,', 'как','дела?']
```
